# Recive and print name

name = input("Enter your name:")                        # Receive a name
print('Hello', name)                                    # Output that name with a 'Hello' within

# Simple Program to calculate pay

hrs = input("Enter Hours:")                             # Receive hours and payrate/h 
rateph = input("Enter rate:")                           # Make the multiplication between those 2  
pay = float(hrs) * float(rateph)                        # Output the result 'Pay'
print ("Pay:", pay)
